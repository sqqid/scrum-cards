services:
  scrumcards:
    container_name: scrumcards
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    environment:
      PORT: "8080"
      ORIGIN: "*"
      LOG_LEVEL: "info"
    restart: unless-stopped
